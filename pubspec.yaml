name: auth_face_recognition
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  opencv_dart: ^1.4.1
  camera: ^0.10.0+1
  path_provider: ^2.0.11
  permission_handler: ^11.3.1
  screen_brightness: ^2.1.1
  http: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/haarcascade_frontalface_default.xml
